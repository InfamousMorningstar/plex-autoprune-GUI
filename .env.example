# =============================================================================
# Plex-Auto-Prune GUI - Environment Configuration Template
# =============================================================================
# 📋 SETUP INSTRUCTIONS:
#   1. Copy this file: cp .env.example .env  (or rename on Windows)
#   2. Edit .env and replace all "your_xxx_here" values with your real data
#   3. Never commit .env to Git - it contains your secrets!
#   4. Keep DRY_RUN=true until you've tested for 1-2 weeks
#
# 🔗 HELPFUL LINKS:
#   • Plex Token: https://support.plex.tv/articles/204059436
#   • Gmail App Password: https://support.google.com/accounts/answer/185833
#   • Tautulli API Key: Tautulli → Settings → Web Interface → API Key
#   • Discord Webhook: Server Settings → Integrations → Webhooks
# =============================================================================

# =============================================================================
# PLEX CONFIGURATION (Required)
# =============================================================================
# Your Plex authentication token (X-Plex-Token)
# How to find: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
PLEX_TOKEN=your_plex_token_here

# Your Plex server name (exactly as shown in Plex settings)
# Example: PLEX_SERVER_NAME=HomeServer
PLEX_SERVER_NAME=My Plex Server

# =============================================================================
# TAUTULLI CONFIGURATION (Required)
# =============================================================================
# Tautulli URL (include http:// or https://)
# Local example: http://192.168.1.100:8181
# Remote example: https://tautulli.yourdomain.com
TAUTULLI_URL=http://localhost:8181

# Tautulli API Key
# Find in: Tautulli → Settings → Web Interface → API Key
TAUTULLI_API_KEY=your_tautulli_api_key_here

# =============================================================================
# EMAIL CONFIGURATION (Required for notifications)
# =============================================================================
# SMTP server hostname
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
SMTP_HOST=smtp.gmail.com

# SMTP port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Your email address
SMTP_USERNAME=your_email@gmail.com

# Email password or App Password
# ⚠️ For Gmail: MUST use App Password (not your regular password)
# Create one: https://support.google.com/accounts/answer/185833
SMTP_PASSWORD=your_app_password_here

# From address (can be same as SMTP_USERNAME or custom display name)
# Example: SMTP_FROM=Plex Server <noreply@plex.local>
SMTP_FROM=your_email@gmail.com

# Admin email for important notifications
ADMIN_EMAIL=admin@example.com

# =============================================================================
# DISCORD NOTIFICATIONS (Optional)
# =============================================================================
# Discord webhook URL for notifications
# Create: Discord Server → Settings → Integrations → Webhooks → New Webhook
# Leave blank to disable Discord notifications
DISCORD_WEBHOOK=

# Discord server invite link (sent to users in emails)
# Example: https://discord.gg/your_invite_code
# Leave blank if you don't have a Discord server
LINK_DISCORD=

# =============================================================================
# USER MANAGEMENT SETTINGS
# =============================================================================
# Days of inactivity before sending warning email
# Default: 27 (users get warned 3 days before removal)
WARN_DAYS=27

# Days of inactivity before removing user
# Default: 30
KICK_DAYS=30

# How often to check for new users (in seconds)
# Default: 120 (2 minutes)
# Lower values = faster detection, more API calls
CHECK_NEW_USERS_SECS=120

# How often to check for inactive users (in seconds)
# Default: 1800 (30 minutes)
# Lower values = more frequent checks, more API calls
CHECK_INACTIVITY_SECS=1800

# =============================================================================
# VIP USERS (Protected from auto-removal)
# =============================================================================
# Comma-separated list of Plex usernames to protect
# These users will NEVER be automatically removed, regardless of activity
# IMPORTANT: Names are case-sensitive and must match Plex exactly
#
# Examples:
#   VIP_NAMES=John,Jane,Bob
#   VIP_NAMES=admin,family_member,close_friend
#
# Leave blank if no VIP users
VIP_NAMES=

# =============================================================================
# TESTING & SAFETY
# =============================================================================
# DRY RUN MODE - Logs actions but doesn't execute them
# ⚠️ IMPORTANT: Keep this as 'true' for testing!
# Only set to 'false' after 1-2 weeks of monitoring logs
#
# true  = Safe mode - no users removed, no emails sent (actions only logged)
# false = Live mode - users WILL be removed and emails WILL be sent
DRY_RUN=true

# =============================================================================
# ADVANCED (Optional - usually don't need to change)
# =============================================================================
# Web UI port (default: 8080)
# Change if port 8080 is already in use
# WEB_PORT=8080

# Secret key for Flask sessions (auto-generated if not provided)
# SECRET_KEY=

# =============================================================================
# NOTES:
# =============================================================================
# • All values are case-sensitive
# • No quotes needed around values
# • No spaces around = sign
# • Lines starting with # are comments and ignored
# • Blank values (empty after =) use defaults or disable features
# =============================================================================

